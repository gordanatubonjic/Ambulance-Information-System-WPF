<Window x:Class="AmbulanceWPF.Views.ReferralDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:models="clr-namespace:AmbulanceWPF.Models" 
        d:DataContext="{d:DesignInstance Type=models:Referral}"
        mc:Ignorable="d"
        Title="Referral Details" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Referral Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Referral Date:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding ReferralDate, StringFormat='MMM dd, yyyy'}" Margin="0,0,0,12"/>

                <TextBlock Text="Referring Doctor:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding ReferringDoctorName}" Margin="0,0,0,12"/>

                <TextBlock Text="Reason:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Reason}" TextWrapping="Wrap" Margin="0,0,0,12"/>

                <TextBlock Text="Specialty:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Specialty}" Margin="0,0,0,12"/>

                <TextBlock Text="Notes:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Notes}" TextWrapping="Wrap" Margin="0,0,0,12"/>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Close" HorizontalAlignment="Right" Margin="0,16,0,0" Click="Close_Click"/>
    </Grid>
</Window>