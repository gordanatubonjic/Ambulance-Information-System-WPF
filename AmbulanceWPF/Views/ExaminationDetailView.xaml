<Window x:Class="AmbulanceWPF.Views.ExaminationDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:models="clr-namespace:AmbulanceWPF.Models" 
        d:DataContext="{d:DesignInstance Type=models:Examination}"
        mc:Ignorable="d"
        Title="Examination Details" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Examination Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Checkup Date:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding CheckupDate, StringFormat='MMM dd, yyyy'}" Margin="0,0,0,12"/>

                <TextBlock Text="Doctor Name:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding DoctorName}" Margin="0,0,0,12"/>

                <TextBlock Text="Summary:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" Margin="0,0,0,12"/>

                <TextBlock Text="Checkup Type:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding CheckupType}" Margin="0,0,0,12"/>

                <TextBlock Text="Detailed Description:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding DetailedDescription}" TextWrapping="Wrap" Margin="0,0,0,12"/>

                <TextBlock Text="Diagnosis:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Diagnosis}" TextWrapping="Wrap" Margin="0,0,0,12"/>

                <TextBlock Text="Medications:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Medications}" TextWrapping="Wrap" Margin="0,0,0,12"/>

                <TextBlock Text="Next Appointment:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding NextAppointment, StringFormat='MMM dd, yyyy', FallbackValue='None'}" Margin="0,0,0,12"/>

                <TextBlock Text="Test Results:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <ItemsControl ItemsSource="{Binding TestResults}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Margin="0,0,0,4"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="Notes:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Notes}" TextWrapping="Wrap" Margin="0,0,0,12"/>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Close" HorizontalAlignment="Right" Margin="0,16,0,0" Click="Close_Click"/>
    </Grid>
</Window>