<Window x:Class="AmbulanceWPF.Views.TechnicianHomePageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AmbulanceWPF.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:userControls="clr-namespace:AmbulanceWPF.Views.UserControls"
       
        mc:Ignorable="d"
        Title="TechnicianHomePageView" Height="600" Width="1000"
        Background="{DynamicResource MaterialDesignPaper}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

                         <userControls:AppHeader Grid.Row="0" Grid.ColumnSpan="2" Margin="0,9,0,9"/>

                         <materialDesign:Card Grid.Row="1" Grid.Column="0" 
                           Margin="16" 
                           VerticalAlignment="Stretch"
                           Background="{DynamicResource MaterialDesignCardBackground}">
                <StackPanel Margin="16">
                    <TextBlock Text="{DynamicResource TechnicianText}" 
                          Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                          Margin="0,0,0,20"/>

                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Content="{DynamicResource MyProfileText}"
                        HorizontalContentAlignment="Left"
                        Margin="0,0,0,10"
                        FontSize="14"
                        Command="{Binding NavigateToInterventionsCommand}"/>

                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Content="{DynamicResource DeliveryText}"
                        HorizontalContentAlignment="Left"
                        Margin="0,0,0,10"
                        FontSize="14"
                        Command="{Binding NavigateToPatientsCommand}"/>

                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Content="{DynamicResource ArchivesText}"
                        HorizontalContentAlignment="Left"
                        Margin="0,0,0,10"
                        FontSize="14"
                        Command="{Binding NavigateToProfileCommand}"/>
                </StackPanel>
            </materialDesign:Card>

                         <Grid Grid.Row="1" Grid.Column="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                                 <materialDesign:Card Grid.Row="0" Margin="0,0,0,16">
                    <TextBox x:Name="SearchTextBox"
                        Margin="16"
                        materialDesign:HintAssist.Hint="{DynamicResource SearchText}"
                        materialDesign:TextFieldAssist.HasLeadingIcon="True"
                        materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                        materialDesign:TextFieldAssist.HasClearButton="True"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        FontSize="14"/>
                </materialDesign:Card>

                                 <StackPanel Grid.Row="1" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16">
                    <Button Style="{StaticResource MaterialDesignFloatingActionButton}"
                        Width="56" Height="56"
                        Margin="8"
                        ToolTip="{DynamicResource ArchivesText}"
                        Command="{Binding NewExamCommand}">
                    <materialDesign:PackIcon Kind="ArchiveSearchOutline" Width="24" Height="24"/>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignFloatingActionButton}"
                        Width="56" Height="56"
                        Margin="8"
                        ToolTip="{DynamicResource NewInterventionText}"
                        Command="{Binding AddInterventionCommand}">
                    <materialDesign:PackIcon Kind="MedicationOutline"  Width="24" Height="24"/>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignFloatingActionButton}"
                        Width="56" Height="56"
                        Margin="8"
                        ToolTip="{DynamicResource MonthlyReportText}"
                        Command="{Binding MonthlyReportCommand}">
                        <materialDesign:PackIcon Kind="ChartBar" Width="24" Height="24"/>
                    </Button>
                </StackPanel>

                                 <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding FilteredPatients}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Margin="0,0,0,12" 
                                               materialDesign:RippleAssist.Feedback="{DynamicResource MaterialDesignBody}"
                                               Cursor="Hand">
                                    <Grid Margin="16">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                                                                 <materialDesign:PackIcon Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                                                           Kind="Account" 
                                                           Width="40" Height="40"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           VerticalAlignment="Top"
                                                           Margin="0,0,16,0"/>

                                                                                 <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{Binding FullName}" 
                                                 FontWeight="Medium" 
                                                 FontSize="16"
                                                 Foreground="{DynamicResource MaterialDesignBody}"/>
                                            <TextBlock Text="{Binding PatientId, StringFormat='(ID: {0})'}" 
                                                 FontSize="14"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 Margin="8,0,0,0"/>
                                        </StackPanel>

                                                                                 <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,8">
                                            <materialDesign:PackIcon Kind="Calendar" Width="16" Height="16" 
                                                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                               VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Age}" 
                                                 FontSize="12"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 Margin="4,0,16,0"/>

                                            <materialDesign:PackIcon Kind="Human" Width="16" Height="16" 
                                                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                               VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Gender}" 
                                                 FontSize="12"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 Margin="4,0,0,0"/>
                                        </StackPanel>

                                                                                 <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                                            <materialDesign:PackIcon Kind="Phone" Width="16" Height="16" 
                                                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                               VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding PhoneNumber}" 
                                                 FontSize="12"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 Margin="4,0,16,0"/>

                                            <materialDesign:PackIcon Kind="MapMarker" Width="16" Height="16" 
                                                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                               VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Address}" 
                                                 FontSize="12"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 Margin="4,0,0,0"
                                                 TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                                                                 <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="MedicalBag" Width="16" Height="16" 
                                                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                               VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding LastVisit, StringFormat='Last visit: {0:MMM dd, yyyy}'}" 
                                                 FontSize="12"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 Margin="4,0,16,0"/>

                                            <materialDesign:Chip Content="{Binding Priority}" 
                                                           FontSize="10"
                                                           Margin="0"
                                                           Height="20">
                                                <materialDesign:Chip.Style>
                                                    <Style TargetType="materialDesign:Chip" BasedOn="{StaticResource {x:Type materialDesign:Chip}}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Priority}" Value="High">
                                                                <Setter Property="Background" Value="#FFEBEE"/>
                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Priority}" Value="Medium">
                                                                <Setter Property="Background" Value="#FFF3E0"/>
                                                                <Setter Property="Foreground" Value="#F57C00"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Priority}" Value="Low">
                                                                <Setter Property="Background" Value="#E8F5E8"/>
                                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </materialDesign:Chip.Style>
                                            </materialDesign:Chip>
                                        </StackPanel>

                                                                                 <Button Grid.Row="0" Grid.Column="2" Grid.RowSpan="4"
                                          Style="{StaticResource MaterialDesignIconButton}"
                                          Command="{Binding DataContext.ViewPatientCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                          CommandParameter="{Binding}"
                                          VerticalAlignment="Center">
                                            <materialDesign:PackIcon Kind="ChevronRight" Width="24" Height="24"/>
                                        </Button>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Window>