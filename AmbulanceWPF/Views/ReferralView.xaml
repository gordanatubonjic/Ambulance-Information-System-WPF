<Window x:Class="AmbulanceWPF.Views.ReferralView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Medical Referral Form" Height="600" Width="500"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="20">
        <materialDesign:Card Padding="20" Margin="0">
            <StackPanel Orientation="Vertical" Margin="0">
                <TextBlock Text="{DynamicResource ReferalFormText}" FontSize="20" FontWeight="Bold" Margin="0 0 0 20" HorizontalAlignment="Center"/>

                                 <TextBlock Text="{DynamicResource PatientInfoText}" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 10"/>

                <TextBox materialDesign:HintAssist.Hint="{DynamicResource FirstNameText}" Margin="0 0 0 10"/>
                <TextBox materialDesign:HintAssist.Hint="{DynamicResource LastNameText}" Margin="0 0 0 10"/>
                <TextBox materialDesign:HintAssist.Hint="{DynamicResource AgeText}" Margin="0 0 0 10" PreviewTextInput="NumberValidationTextBox"/>
                <ComboBox materialDesign:HintAssist.Hint="{DynamicResource GenderText}" Margin="0 0 0 10">
                    <ComboBoxItem Content="Male"/>
                    <ComboBoxItem Content="Female"/>
                    <ComboBoxItem Content="Other"/>
                </ComboBox>
                <DatePicker materialDesign:HintAssist.Hint="{DynamicResource DateOfBirthText}" Margin="0 0 0 20"/>

                                 <TextBlock Text="{DynamicResource ReferalDetailsHeaderText}" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 10"/>

                <ComboBox materialDesign:HintAssist.Hint="{DynamicResource SpecialtyDoctorText}" Margin="0 0 0 10">
                    <ComboBoxItem Content="Cardiology"/>
                    <ComboBoxItem Content="Neurology"/>
                    <ComboBoxItem Content="Orthopedics"/>
                    <ComboBoxItem Content="Pediatrics"/>
                    <ComboBoxItem Content="Oncology"/>
                    <ComboBoxItem Content="Other"/>
                </ComboBox>
                <TextBox materialDesign:HintAssist.Hint="{DynamicResource ReferalReasonText}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="100" Margin="0 0 0 10"/>
                <TextBox materialDesign:HintAssist.Hint="{DynamicResource ReferingDoctorText}" Margin="0 0 0 20"/>

                                 <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="{DynamicResource SubmitText}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0 0 10 0"/>
                    <Button Content="{DynamicResource CancelText}" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
