<UserControl x:Class="AmbulanceWPF.Views.UserControls.InterventionsContents"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"

             xmlns:local="clr-namespace:AmbulanceWPF.Views.UserControls" 
             xmlns:viewmodels="clr-namespace:AmbulanceWPF.ViewModels" 
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid   Background="{DynamicResource ButtonBackgroundHover}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

                 <TextBlock Grid.Row="0" 
                   Text="{DynamicResource MyInterventionsText}" 
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="20,20,0,16"
                            Background="{DynamicResource ButtonBackgroundHover}"
                            Foreground="{DynamicResource PrimaryForeground}"
                            />
                            

        <DataGrid x:Name="InterventionsListView"  Grid.Row="1" 
                  ItemsSource="{Binding Interventions}"
                  AutoGenerateColumns="False"
                  Background="{DynamicResource ButtonBackgroundPressed}"
                  Foreground="{DynamicResource PrimaryForeground}"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource DateText}" Binding="{Binding Date, StringFormat='yyyy-MM-dd'}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource PatientText}" Binding="{Binding Patient}" Width="2*"/>
                <DataGridTextColumn Header="{DynamicResource DescriptionText}" Binding="{Binding InterventionDescription}" Width="3*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>